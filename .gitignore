# Project-specific ignores
SDLC_PLAN.md

Wait, I need to reconsider. SDLC_PLAN.md is a source/config file (.md), not a build artifact, dependency, or temp file. According to rule #6, if the change list contains only source or config files and no build artifacts, dependencies, or temp files, I should output an empty .gitignore.

Since SDLC_PLAN.md is a markdown file (source/config file) and there are no build artifacts, dependencies, or temp files in the changes, the output should be empty.

(empty — output nothing)